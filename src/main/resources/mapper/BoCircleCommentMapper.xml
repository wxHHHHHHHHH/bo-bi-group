<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boss.bobi.boCircle.mapper.BoCircleCommentMapper">

    <resultMap id="BaseResultMap" type="com.boss.bobi.boCircle.entity.BoCircleComment">
            <id property="id" column="id" jdbcType="INTEGER"/>
            <result property="userId" column="userId" jdbcType="INTEGER"/>
            <result property="boCircleId" column="bo_circle_id" jdbcType="INTEGER"/>
            <result property="content" column="content" jdbcType="VARCHAR"/>
            <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,initiator_id,reply_id,
        content,create_time
    </sql>
    <select id="findBoCircleCommentInfo" resultType="com.boss.bobi.boCircle.model.CommentUserVo">
        SELECT u.id as "userId",u.nick_name,b.id,b.content FROM  bo_circle_comment b INNER JOIN user u ON b.user_id = u.id WHERE b.bo_circle_id = #{id} ORDER BY b.create_time asc
    </select>
</mapper>
