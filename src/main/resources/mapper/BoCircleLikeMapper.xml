<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boss.bobi.boCircle.mapper.BoCircleLikeMapper">

    <resultMap id="BaseResultMap" type="com.boss.bobi.boCircle.entity.BoCircleLike">
            <id property="id" column="id" jdbcType="INTEGER"/>
            <result property="userId" column="user_id" jdbcType="INTEGER"/>
            <result property="boCircleId" column="bo_circle_id" jdbcType="INTEGER"/>
            <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,user_id,bo_circle_id,
        create_time
    </sql>
    <select id="findBoCircleLikeInfo" resultType="com.boss.bobi.boCircle.model.LikeUserVo">
        SELECT u.id as "userId",u.nick_name FROM  bo_circle_like b INNER JOIN user u ON b.user_id = u.id WHERE b.bo_circle_id = #{id} ORDER BY b.create_time asc
    </select>
</mapper>
